{
  "id": "edit-file",
  "description": "Edit an existing file through the provider-native VFS edit tool.",
  "prompt": "Use the available VFS exact-edit tool on docs/plan.txt with old text draft and new text final. Replace only the single occurrence. Then answer exactly EDIT=final.",
  "setup": {
    "files": [
      {
        "path": "docs/plan.txt",
        "content": "phase: draft\n"
      }
    ]
  },
  "expect": {
    "tool_called": ["vfs.edit_file"],
    "assistant_contains": ["EDIT=final"],
    "tool_output_contains": ["Replaced 1 match"],
    "files": [
      {
        "path": "docs/plan.txt",
        "contains": "phase: final"
      }
    ]
  },
  "eval": {
    "runs": 1,
    "min_pass_rate": 1.0
  },
  "run": {
    "max_tokens": 768,
    "allowed_tools": ["vfs.edit_file"]
  }
}
