/// Product surfaces that exist in the codebase but are not ready to show.
/// Flip to re-enable; the routes and navigation are gated on these flags so
/// the code stays compiled and typechecked while hidden.
export const FOUNDRY_ENABLED = false;
